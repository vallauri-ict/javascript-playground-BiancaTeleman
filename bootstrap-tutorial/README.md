BootStrap-tutorial:
- In questo esercizio introduttivo al Bootstrap è stato spiegato il come caricare il   bootstrap all interno del <head> della pagina HTML
- Successivamente è stato caricato prima della chiusura del <body> la parte relative al   jQuery e al JavaScript
- Dopo ho scritto all'interno del <body> la parte relativa alla classe <navbar>(è la barra    di navigazione che è responsive) che a sua volta può avere diverse carratteristiche come:
   -navbar-expand- *dimensione come md o sm etc* che indica a che grandezze deve        rimpiciolirsi
   -Le varie funzioni per le icone e il logo del sito
- Poi ho scritto il codice relativo all' <header> con anche la classe contentor-fluid
- Bootstrap inoltre ha la possibilità di usare delle colonne e per "dimensionarle" si assegna a loro il numero delle colonne che occupa + la grandezza(md,sm etc). 